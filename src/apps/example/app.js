export default {
    name: "example",
    startX: 100,
    startY: 100,
    url: "http://127.0.0.1:8080/src/apps/example/index.html", //we don't have to have the real page in our repo.
    icon: "http://127.0.0.1:8080/src/apps/example/example_icon.png",
    // we can just to them with urls. here is on the repo tho.

    onOpen: function(window){},
    onOpenWith: function(window, files){},
    
    // icon: "./img/icon"
}